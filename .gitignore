# --- docs policy: every .md is internal unless whitelisted below ---
*.md
!README.md
!CLAUDE.md
!COMMANDS.md
!CHANGELOG.md
!docs/**/*.md
!benchmarks/**/*.md
# plan2-*.md and session*-addendum.md are covered by *.md above: never tracked.

# --- secrets / local ---
.env
.env.*
!.env.example
*.pem
*.key

# --- python ---
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
dist/
build/
.pytest_cache/
.hypothesis/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# --- rust ---
target/
Cargo.lock.bak

# --- data / benchmarks (raw data never committed; small fixtures and reports are) ---
data/
*.parquet
*.feather
*.arrow
*.ipc
*.csv
*.tsv
*.json
*.jsonl
*.xlsx
*.xls
*.pkl
*.pickle
*.npy
*.npz
*.h5
*.hdf5
*.arff
*.zip
*.gz
*.gzip
*.zst
*.zstd
*.bz2
*.xz
*.7z
*.tar
!tests/fixtures/**
# generated benchmark outputs (report.json / report.html) stay local: reproducibility comes
# from seeds and recipes, not committed blobs. Only the tiny pin survives.
!benchmarks/**/pinned.json

# --- editors / os ---
.vscode/
.idea/
.DS_Store
Thumbs.db

# --- build artifacts (the extension is built by uv sync / maturin, never committed) ---
*.so
*.pyd
*.dylib

# --- personal scratch: benchmarks, proofs, experiments. Never tracked. ---
scratch/

# --- the report suite (schema, TypeScript source, checked-in build) is code, not data ---
!report/**
report/node_modules/
benchmarks/**/report.html
